# psn-app-v3

Aplikacja SPA (HTML/JS/CSS) do wystawiania zezwoleń PSN, z backendem Node/Express + JWT i generowaniem PDF.

## 🧩 Wymagania
- Node.js 18+  
- Przeglądarka (Chrome/Edge/Firefox)  
- GitHub Pages (opcjonalnie do hostingu frontendu)

---

# 🚀 URUCHOMIENIE APLIKACJI

## 1. Backend (API)
W katalogu `backend/`:

```bash
cd backend
cp .env.example .env   # lub skopiować ręcznie jako .env
npm install
npm start
